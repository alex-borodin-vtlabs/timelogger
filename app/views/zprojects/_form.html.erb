<%= form_for(@zproject) do |f| %>
  <% if @zproject.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@zproject.errors.count, "error") %> prohibited this zproject from being saved:</h2>

      <ul>
      <% @zproject.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
